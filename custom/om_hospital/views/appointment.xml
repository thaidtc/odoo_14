<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Tree View -->
    <record id="hospital_appointment_tree" model="ir.ui.view">
        <field name="name">hospital.appointment.tree</field>
        <field name="model">hospital.appointment</field>
        <field name="arch" type="xml">
            <tree string="Appointments" default_order="name asc" create="true">
                <field name="patient_id" string="Patient"/>
                <field name="date_appointment" string="Date"/>
                <field name="date_checkup" string="Check Up Time"/>
                <field name="state" string="Status"/>
                <field name="note" string="Description"/>
            </tree>
        </field>
    </record>

    <!-- Kanban View -->
    <record id="hospital_appointment_kanban" model="ir.ui.view">
        <field name="name">hospital.appointment.kanban</field>
        <field name="model">hospital.appointment</field>
        <field name="arch" type="xml">
            <kanban>
                <field name="patient_id" string="Patient"/>
                <field name="date_appointment" string="Date"/>
                <field name="date_checkup" string="Check Up Time"/>
                <field name="state" string="Status"/>
                <field name="note" string="Description"/>
                <templates>
                    <t t-name="kanban-box">
                        <div t-attf-class="oe_kanban_global_click">
                            <div class="oe_kanban_details">
                                <ul>
                                    <li>
                                        Patient: <field name="patient_id"/>
                                    </li>
                                    <li>
                                        Date: <field name="date_appointment"/>
                                    </li>
                                    <li>
                                        Check Up Time: <field name="date_checkup"/>
                                    </li>
                                    <li>
                                        State: <field name="state"/>
                                    </li>
                                    <li>
                                        Note: <field name="note"/>
                                    </li>
                                </ul>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Form View -->
    <record id="hospital_appointment_form" model="ir.ui.view">
        <field name="name">hospital.appointment.form</field>
        <field name="model">hospital.appointment</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button id="button_confirm" name="action_confirm" string="Confirm" class="btn-primary" states="draft" confirm="Are you sure that you need to confirm?" type="object" />
                    <button id="button_done" name="action_done" string="Mark As Done" class="btn-primary" states="confirm" type="object" />
                    <button id="button_draft" name="action_draft" string="Set To Draft" class="btn-primary" states="cancel" type="object" />
                    <button id="button_cancel" name="action_cancel" string="Cancel" states="draft,confirm,done" confirm="Are you sure that you need to cancel?" type="object" />
                    <field name="state" widget="statusbar" statusbar_visible="draft,confirm,done" />
                </header>

                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" readonly="1"/>
                        </h1>
                    </div>

                    <group>
                        <field name="patient_id"/>
                        <field name="date_appointment"/>
                        <field name="date_checkup"/>
                        <field name="note"/>
                    </group>
                </sheet>

                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <!-- Search View -->
    <record id="hospital_appointment_search" model="ir.ui.view">
        <field name="name">hospital.appointment.search</field>
        <field name="model">hospital.appointment</field>
        <field name="arch" type="xml">
            <search string="Appointments">
                <field name="name"/>
                <field name="patient_id"/>
                <separator />

                <group expand="1" string="Group By">
                    <filter string="Patient" name="patient_id" context="{'group_by': 'patient_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action -->
    <record id="appointment_action" model="ir.actions.act_window">
        <field name="name">Appointments</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">hospital.appointment</field>
        <field name="view_mode">tree,kanban,form</field>
        <field name="context">{}</field>

        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first appointment!
            </p>
        </field>
    </record>

    <menuitem id="hospital_appointment_root" name="Appointments" parent="hospital_root" sequence="20" />

    <menuitem id="hospital_appointment" name="Appointments" parent="hospital_appointment_root" action="appointment_action" sequence="10" />
</odoo>
