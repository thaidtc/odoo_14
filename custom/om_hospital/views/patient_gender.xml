<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Action -->
    <record id="patient_male_action" model="ir.actions.act_window">
        <field name="name">Male</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">hospital.patient</field>
        <field name="view_mode">tree,kanban,form</field>
        <field name="context">{'default_gender': 'male', 'default_note': 'New Patient', 'hide_gender': 1}</field>
        <field name="domain">[('gender','=', 'male')]</field>

        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first patient!
            </p>
        </field>
    </record>

    <record id="patient_female_action" model="ir.actions.act_window">
        <field name="name">Female</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">hospital.patient</field>
        <field name="view_mode">tree,kanban,form</field>
        <field name="context">{'default_gender': 'female', 'default_note': 'New Patient', 'hide_gender': 1}</field>
        <field name="domain">[('gender','=', 'female')]</field>

        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first patient!
            </p>
        </field>
    </record>

    <menuitem id="hospital_patient_male" name="Male" parent="hospital_patient_root" action="patient_male_action" sequence="30" />
    <menuitem id="hospital_patient_female" name="Female" parent="hospital_patient_root" action="patient_female_action" sequence="40" />
</odoo>
